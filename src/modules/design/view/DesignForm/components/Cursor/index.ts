import Cursor from './Cursor';
import CursorWrapper from './Cursor.Wrapper';
import LiveCursor from './Cursor.live';

const Cursors = {
  Cursor,
  Live: LiveCursor,
  Wrapper: CursorWrapper,
};

export default Cursors;
